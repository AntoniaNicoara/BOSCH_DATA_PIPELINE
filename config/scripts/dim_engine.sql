SELECT
    engine_id as engine_id,
    Manufacturer as manufacturer,
    Origin as origin,
    power_source as power_source,
    fuel_configuration as fuel_configuration,
    vehicle_ids as vehicle_ids
FROM dev.silver.heavy_duty_engines_silver